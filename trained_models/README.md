# Trained Models

Local model artifacts live here and are mounted into the backend container by Docker Compose.

Default ensemble path:

```text
trained_models/ensemble_models_2_3000/manifest.json
```

Model weights are ignored by git.

