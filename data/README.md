# Data

- `train_data/` contains raw and processed datasets used for training and evaluation.
- `output_data/` contains generated analysis reports and visual artifacts.

Datasets and generated outputs are ignored by git.

